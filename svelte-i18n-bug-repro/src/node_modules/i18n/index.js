import { getLocaleFromPathname, init, register } from 'svelte-i18n';

register('en', () => import(`../../../messages/en.json`));
register('ga', () => import(`../../../messages/ga.json`));

init({
    fallbackLocale: 'en',
    initialLocale: getLocaleFromPathname(/^\/([^\/]*).*$/)
});